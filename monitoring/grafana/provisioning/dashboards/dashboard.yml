# Provisioning dashboard untuk Grafana.
apiVersion: 1

providers:
  - name: "MLOps"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards